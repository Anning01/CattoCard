# Card Store Configuration
# Copy this file to .env and fill in the values

# ==================== 应用配置 ====================
APP_NAME=Card Store
APP_VERSION=0.1.0
DEBUG=false

# ==================== 数据库配置 ====================
# 本地开发（SQLite）
# DATABASE_URL=sqlite://./data/cardstore.db

# 本地开发（PostgreSQL）
# DATABASE_URL=postgres://user:password@localhost:5432/cardstore

# Docker 部署（PostgreSQL）
POSTGRES_USER=cardstore
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=cardstore

# ==================== Redis 配置 ====================
# 不配置则使用内存缓存，配置后支持支付金额映射和分布式锁
# 本地开发
# REDIS_URL=redis://localhost:6379/0
# Docker 部署会自动配置

# ==================== JWT 配置 ====================
# 请生成一个安全的随机密钥：openssl rand -base64 32
SECRET_KEY=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# ==================== 文件上传配置 ====================
UPLOAD_DIR=uploads
UPLOAD_MAX_SIZE=10485760
STATIC_URL_PREFIX=/api/static

# ==================== 其他配置 ====================
DEFAULT_CURRENCY=CNY

# ==================== 说明 ====================
# 邮件配置已移至后台管理界面（基础配置 -> 邮件配置）
# 支付方式配置已移至后台管理界面（商品管理 -> 支付方式）
# 站点配置已移至后台管理界面（基础配置 -> 站点设置）
